import { HomeView } from "@/components/dashboard/views/HomeView";

// Home — the default dashboard view. Data comes from the layout's provider.
export default function DashboardHomePage() {
  return <HomeView />;
}
