# Phase 0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Execute Phase 0 of `docs/DEVELOPMENT-PLAN.md` — Track B (amendment-pipeline repair, docs) ∥ Track A (Wave A engine fixes).

**Architecture:** Track B is one docs task on branch `phase0-amendment-pipeline-2026-07-13` (this branch; spec+plan committed here). Track A runs on branch `phase0-wave-a-2026-07-13` off main, tasks strictly sequential (shared engine files + golden master), smallest/safest first.

**Tech Stack:** Markdown (Track B); JS engine/app code + node test runner (Track A). Suite: `npm test` (197 files) + `npm run lint` (0 errors required).

## Global Constraints

1. Golden master discipline: a re-baseline happens ONLY when the task says so; run `UPDATE=1 node apps/mobile/tests/golden-master.js`, then audit the diff archetype-by-archetype and record an **expected-delta note** in the commit message (which archetypes moved and why). Any archetype outside the task's stated blast radius = STOP, report BLOCKED.
2. Engine purity: no clock/randomness/I-O in `packages/engine` plan paths (ESLint purity rules enforce; keep them green).
3. Sports/cohorts outside a task's stated blast radius stay **byte-identical**.
4. `npm test` and `npm run lint` green before every commit; commit per task, message `fix(engine): …` / `docs(governance): …` ending `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
5. Detail authority: each task cites the audit deliverable that owns its finding — read it before coding. Paths: `docs/reviews/2026-07-11-engine-audit-0N-*.md`.
6. No frozen doc edited. `docs/DOCUMENTATION-GOVERNANCE.md` and `docs/DOCUMENTATION-INDEX.md` are LIVING (editable). P1-10's actual DB apply is NOT executed — prepare + pause.

---

### Task B1: Amendment pipeline (GA-702, GA-703, GA-701)

**Files:** Create `docs/AMENDMENT-QUEUE.md` · Modify `docs/DOCUMENTATION-GOVERNANCE.md`, `docs/DOCUMENTATION-INDEX.md`
**Branch:** `phase0-amendment-pipeline-2026-07-13`

- [ ] Read governance audit 07 (`…governance-audit-07-doc-governance.md`) §4 GA-701/702/703 (the findings + directions) and audit 09 §3 (the AQ/ND entries the register must track); read `docs/DOCUMENTATION-GOVERNANCE.md` in full.
- [ ] Create `docs/AMENDMENT-QUEUE.md` (banner: WORKING (T4) · the amendment queue's single home): a lifecycle table (`Item | Source (evidence link) | Target doc | Status: QUEUED/BATCHED/RATIFIED/REJECTED | Notes`) seeded with C1–C5 (source: 2026-07-09 documentation audit §2) and AQ-1…AQ-9 + ND-1 (source: governance audit 09 §3) — pointers only, no content restated.
- [ ] Edit `docs/DOCUMENTATION-GOVERNANCE.md`: §3's "queue in the documentation audit" pointer now points at the register (resolving the GA-702 contradiction); add a "Ratification (T2 entry)" subsection (GA-703: proposal → adversarial panel → Simon ratifies → status+index flip in one change; applies to AIGAS now, ND-1 later) and a "Batch amendments" subsection (GA-701: one written set, whole-set consistency review, one version bump per doc, Simon ratifies the batch).
- [ ] Add the register to `docs/DOCUMENTATION-INDEX.md` T4 table.
- [ ] Verify: `grep -c "AQ-" docs/AMENDMENT-QUEUE.md` ≥ 9; queue/ratification/batch sections present; no frozen file in `git diff --name-only`.
- [ ] Commit `docs(governance): amendment queue register + ratification & batch protocols (GA-701/702/703)`; push; PR titled `docs(governance): Phase 0 Track B — repair the amendment pipeline`.

### Task A1: P0-6 — quality-projection mapping bug + droppedDemands

**Branch (A-tasks):** `phase0-wave-a-2026-07-13` (create from main).
**Read first:** engine-audit 04 §B3 + audit 09 item P0-6. Key files: `packages/engine/src/lib/performance/sportQualityMap.js` (the fixed-10 projection; `strengthEndurance` lacks its identity mapping), `demandProfile.js:17`.
- [ ] TDD: test that a sport whose SKB authors `strengthEndurance` demand projects it into the demand profile (it is a PM quality — identity mapping), and that any authored-but-unprojected quality lands in a `droppedDemands` list on the profile/meta (honesty ledger, Art 15).
- [ ] Fix the mapping; emit `droppedDemands`. Blast radius: sports whose SKB authors the affected qualities — audit expected rugby/hurling changes; goldens re-baselined ONLY if diagnosis output actually shifts, with expected-delta note.
- [ ] Suite + lint green; commit.

### Task A2: P0-7 — plan-memo staleness

**Read first:** audit 06 TR-06. File: `apps/mobile/src/lib/PlanService.js:327-341` (`profileSignature`).
- [ ] TDD: signature changes when `sport_code`, `first_game_date`, `last_game_date`, or `athlete_model` change.
- [ ] Add the fields. No golden impact (app-side memo only). Suite + lint; commit.

### Task A3: P0-1 — style-band fallthrough

**Read first:** audit 06 TR-01 + audit 09 P0-1. Files: `packages/engine/src/lib/strength/targets.js:56` (`STYLE_TOP` via KB `volume.style_top`), `packages/engine/src/data/doseSchemes.js:113-119` (`STYLE_SCHEME_BRIDGE`), `packages/engine/src/lib/plan/allocator.js:642` (whitelist).
- [ ] TDD: powerlifting profile gets the powerlifting volume band (0.6 + 3-primary cap), hypertrophy gets its 1.4 overreach band — not functional's.
- [ ] Key the three tables on the discipline ids (`powerlifting`/`hypertrophy`/`olympic`); keep legacy keys only if a live caller still passes them (check, don't assume).
- [ ] Blast radius: the ~12 build archetypes ONLY; sports byte-identical. Deliberate re-baseline + expected-delta note. Suite + lint; commit.

### Task A4: P0-2 — injury honesty set

**Read first:** audit 06 TR-04, audit 07 SR-03. Files: `packages/engine/src/lib/injury/injuryFilter.js:47` (stamps `discipline:'rehab'`), `packages/engine/src/lib/validation/validators.js:24` (filters to gym → rehab invisible), `packages/engine/src/lib/plan/volume.js:81-93` (hidden struck items counted).
- [ ] TDD: (1) a rehab-stamped session IS seen by the validator suite; (2) a region with no rehab content yields an explicit `unservable` outcome (surfaced, Art 15) — never an empty session with a "replaced with rehab" banner; (3) struck/hidden items do not count toward weekly volume.
- [ ] Implement. Blast radius: injured-athlete paths only; goldens (uninjured archetypes) byte-identical — if any golden moves, STOP/BLOCKED. Suite + lint; commit.

### Task A5: P0-3 — injury-veto gate behind a flag

**Read first:** audit 09 P0-3; EDS §37 conflict order (validators may veto). Depends on A4 (rehab visible).
- [ ] TDD: with flag ON (e.g. `validation.enforceInjuryVetoes` option/env default OFF), a plan item matching an active contraindication is vetoed at D14 (removed + recorded in the report); with flag OFF, report-only (today's behaviour, byte-identical).
- [ ] Implement gate + flag; default OFF everywhere (promotion = Simon's I5). Goldens untouched. Suite + lint; commit.

### Task A6: P0-5 — legacy-cohort rescue

**Read first:** audit 04 B1, audit 01 §3 (cohort map), audit 09 P0-5. File: `packages/engine/src/lib/plan/allocator.js:87-100` (`diagnosisSteers`, `D11_SPORTS`).
- [ ] TDD first for each cohort: triathlon, zero-gap run/cycle, code-less GAA profiles produce diagnosis-steered plans (meta.diagnosis present; selection value-ordered, not deficit-fill).
- [ ] Open the gate for those cohorts (smallest change that routes them through the D11 path — likely extending the gate predicate; do NOT delete the legacy fill — that is M2).
- [ ] Blast radius: exactly those cohort archetypes; every other archetype byte-identical. Deliberate re-baseline + expected-delta note, key-by-key audit. Coaching-quality sanity: rescued plans must thread priority work per session (compare vs audit 03's quality bars); if output looks degraded, STOP/BLOCKED for review rather than shipping worse plans.
- [ ] Suite + lint; commit.

### Task A7: P1-10 — prepare the staging apply, then PAUSE ⚠

- [ ] Verify `supabase/migrations/20260712_player_status_membership_scope.sql` exists and read `supabase/SECURITY-DEPLOY.md`'s runbook; write the exact ordered commands (staging apply → `node supabase/tests/rls-harness.mjs` expecting the 7 F3 cases → prod apply) into the PR description.
- [ ] **STOP. Do not execute against any database.** Simon runs or explicitly green-lights the runbook.

### Task A8: Wave A wrap

- [ ] Whole-branch review (superpowers:requesting-code-review), fix findings, HANDOFF update (Phase 0 status), push, PR `fix(engine): Phase 0 Track A — Wave A stop-the-bleeding (P0-1..P0-7)`. **Simon merges.**
