# @performance-os/shared (placeholder)

Reserved slot for code shared across apps (mobile + future web):

- `types/` — shared data types / shapes.
- `utils/` — small cross-cutting helpers.
- `statusLogic/` — shared status / state derivation logic.

**Empty for now.** Code will be moved here only when there is something genuinely
shared between `apps/mobile` and `apps/web`. Nothing has been extracted yet.
